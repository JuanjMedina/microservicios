// Configuración común para todos los subproyectos
allprojects {
    group = 'com.juan'
    version = '0.0.1-SNAPSHOT'
}

subprojects {
    apply plugin: 'java'

    repositories {
        mavenCentral()
    }

    dependencies {
    }
}
